def letter_removal(word,position):
  if position < 0 or position > len(word):
    return -1
  else:
    word = list(word)
    del word[position]
    return''.join(word)

word = input('Enter a word: ')
position = int(input('Enter a position: '))
print(letter_removal(word,position))
